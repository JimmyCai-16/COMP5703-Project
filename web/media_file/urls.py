import os

from django.urls import path, re_path, include
from django.conf import settings
from . import views

urlpatterns = [
    # path('download/<str:path>', views.download_view, name='download'),
]
